spring:
  profiles:
    active:
      dev
    group:
      local-env:
        - local
      dev-env:
        - dev
      prod-env:
        - prod
    include:
      - jwt
      - oauth
server:
  port: 9090